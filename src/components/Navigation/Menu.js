import React from 'react'
import classes from './Menu.css'

const menu = (props) =>(
<div className={classes.BurgerIcon} onClick={props.click}>
    <p></p>
    <p></p>
    <p></p>
</div>
);


export default menu;
